box: node:10
build:
  steps:
    - script:
        name: npm install
        code: |
          npm i xo semantic-release

    - script:
      name: xo-lint
      code: |
        npx xo src/*.js

    - script:
      name: Release
      code: |
        npm run package
        npx semantic-release
